# 指標方法與使用限制

本專案沒有經過訓練的機器學習模型。沿用 `MODEL_CARD.md` 作為方法、資料與限制的統一入口。

## 用途與資料

- 用於探索單一股票的歷史價格、均線趨勢與標準差偏離。
- yfinance 向 Yahoo Finance 取得 `auto_adjust=True` 的調整後 Close；不是逐筆即時報價。
- 日期範圍的 `end` 依 Yahoo/yfinance 慣例為不包含當日的上界。
- 資料只在請求時下載並計算；目前沒有自行建立歷史資料庫。

## 計算定義

預設值定義於 `configs/dashboard.toml`：

| 指標 | 定義 |
| --- | --- |
| MA | 最近 200 筆交易日收盤價的平均 |
| 標準差 | 相同 200 筆價格的樣本標準差，`ddof=1` |
| Z-Score | `(Close - MA) / 標準差` |
| 價格區間 | `MA ± 1.5 × 標準差` |
| 年化斜率 | 最近 21 筆 MA 的線性回歸斜率 × 252 / 當期 MA × 100 |

年化斜率是趨勢尺度，不是 CAGR、實際報酬率或未來報酬預測。
均線至少需要 200 筆有效觀測，第一個完整斜率需 220 筆。變更日期起點也會改變暖機區間。

## 標籤與相容性

- Z-Score 標籤依絕對值：`<1.5` 為 NEUTRAL、`1.5–<2.5` 為 ELEVATED、`>=2.5` 為 EXTREME。
- 斜率標籤：`>15%` 為 STRONG UPTREND、`>0%` 為 UPTREND，其餘為 DOWNTREND。
- 介面的斜率色彩使用 ±5% 分界，與文字標籤的分界不同，沿用既有呈現。
- API 保留 `ma200`、`price_chart` 等原有欄位名稱，即使使用者自訂均線窗口。
- 保留原版 KPI 行為：沒有可用 Z-Score／斜率時顯示 0；尾端缺值時取最後可用值。圖表中的未定義數值則為 `null`。因此 0 不一定代表真正中性或零斜率，應同時查看圖表與資料日期。

## 資料洩漏與限制

公式只使用當日及以前的輸入資料，未使用置中窗口、向後填值或未來資料。`tests/test_no_leakage.py` 以改變未來價格和截短歷史檢查此性質。

但 Yahoo 的調整後歷史價格可能因拆股、股利或資料更正而回溯修訂；公式沒有使用未來輸入，不等於資料具有嚴格 point-in-time 保證。若要回測策略，需另外處理當時可得資料、交易成本、滑價與存活者偏差。

價格分布未必符合常態分布，Z-Score 不可直接換算成獲利機率。無法據此判定內在價值、破產風險或適合個人的投資決策。

本次驗證涵蓋數值、相容性與執行流程；沒有宣稱預測準確率、策略報酬或大量同時使用者的服務能力。詳見 [驗證報告](reports/validation.md)。
